function initEmailJS(){if(typeof CONFIG==='undefined'||!CONFIG.emailjs?.publicKey)return;emailjs.init(CONFIG.emailjs.publicKey);}
function setupContactForm(){const form=document.getElementById('contact-form');if(!form)return;form.addEventListener('submit',e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]'),resp=grecaptcha.getResponse();if(!resp.length){alert("Verifica que no eres un robot");btn.disabled=false;return;}btn.disabled=true;emailjs.send(CONFIG.emailjs.serviceId,CONFIG.emailjs.templateId,{from_name:document.getElementById('name').value,reply_to:document.getElementById('email').value,message:document.getElementById('message').value,to_name:"RS Revolution","g-recaptcha-response":resp}).then(()=>{form.reset();grecaptcha.reset();alert("¡Mensaje enviado!");btn.disabled=false;},()=>{grecaptcha.reset();alert("Error al enviar.");btn.disabled=false;})});}
document.addEventListener('DOMContentLoaded',()=>{initEmailJS();setupContactForm();});
